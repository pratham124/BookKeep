import React from "react";
import "./Spinner.css"; // Ensure you import the CSS for styling

const Loader = () => (
  <div className="spinner-container">
    <div className="loading-spinner"></div>
  </div>
);

export default Loader;
